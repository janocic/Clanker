---
---

<!doctype html>
<html lang="hr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Clanker</title>
  </head>
  <body>
    <div class="ambient-pulse" aria-hidden="true"></div>

    <div class="app-shell">
      <aside class="sidebar">
        <div class="brand">CLANKER</div>

        <nav class="nav">
          <button class="nav-item active" data-view="overview">[ PREGLED ]</button>
          <div class="nav-label">// UREDAJI</div>
          <div id="device-nav"><span class="nav-empty">nema uredaja</span></div>
        </nav>

        <div class="sidebar-status">
          <span class="brand-dot" id="live-dot"></span>
          <span id="clock">--:--:--</span>
        </div>
      </aside>

      <div class="content">
        <header class="topbar">
          <div class="breadcrumb" id="breadcrumb">PREGLED</div>
        </header>

        <main id="view-overview">
          <section class="stats">
            <div class="term card">
              <div class="card-label">Uredaji</div>
              <div class="card-value" id="stat-devices">0</div>
            </div>
            <div class="term card">
              <div class="card-label">Upiti ukupno</div>
              <div class="card-value" id="stat-total">0</div>
            </div>
            <div class="term card card-danger">
              <div class="card-label">Blokirano upita</div>
              <div class="card-value" id="stat-blocked">0</div>
            </div>
            <div class="term card card-warn">
              <div class="card-label">Sumnjivih uredaja</div>
              <div class="card-value" id="stat-suspicious">0</div>
            </div>
            <div class="term card card-warn">
              <div class="card-label">Odspojeno uredaja</div>
              <div class="card-value" id="stat-cut">0</div>
            </div>
          </section>

          <section class="charts-row">
            <div class="term panel chart-panel">
              <div class="panel-head">
                <h2>[ BLOKIRANO PO UREDAJU ]</h2>
              </div>
              <div class="chart-body" id="bar-chart"></div>
            </div>
            <div class="term panel chart-panel">
              <div class="panel-head">
                <h2>[ UKUPNI PROMET ]</h2>
                <span class="panel-hint">svi uredaji, zadnjih ~60s</span>
              </div>
              <div class="chart-body" id="area-chart"></div>
            </div>
          </section>

          <section class="term panel">
            <div class="panel-head">
              <h2>[ LIVE BLOCKLIST ]</h2>
              <span class="panel-hint">dodaj domenu odmah, bez restarta</span>
            </div>
            <div class="blocklist-form">
              <input
                type="text"
                id="domain-input"
                placeholder="npr. neka-nova-ai-stranica.com"
                autocomplete="off"
                spellcheck="false"
              />
              <button class="btn btn-accent" id="ban-btn">BANNAJ</button>
            </div>
            <div class="chip-row" id="live-chips">
              <span class="chip-empty">Ucitavam...</span>
            </div>
          </section>

          <section class="term panel">
            <div class="panel-head">
              <h2>[ UREDAJI ]</h2>
              <span class="panel-hint">strelica = log uredaja &middot; IP = detalji</span>
            </div>
            <div class="table-wrap">
              <table>
                <thead>
                  <tr>
                    <th></th>
                    <th>IP</th>
                    <th>MAC</th>
                    <th>Promet</th>
                    <th>Upiti</th>
                    <th>Blokirano</th>
                    <th>Status</th>
                    <th></th>
                  </tr>
                </thead>
                <tbody id="devices-body">
                  <tr class="empty-row"><td colspan="8">Cekam uredaje...</td></tr>
                </tbody>
              </table>
            </div>
          </section>
        </main>

        <main id="view-device" hidden>
          <div class="device-header">
            <div>
              <h2 id="device-title">--</h2>
              <div class="mono-dim" id="device-mac">--</div>
            </div>
            <div class="device-header-right">
              <div id="device-badges"></div>
              <button class="btn" id="device-action-btn" data-action="toggle-block">--</button>
            </div>
          </div>

          <section class="stats stats-compact">
            <div class="term card">
              <div class="card-label">Upiti</div>
              <div class="card-value" id="device-stat-total">0</div>
            </div>
            <div class="term card card-danger">
              <div class="card-label">Blokirano</div>
              <div class="card-value" id="device-stat-blocked">0</div>
            </div>
            <div class="term card">
              <div class="card-label">Trenutni promet</div>
              <div class="card-value" id="device-stat-rate">0 B/s</div>
            </div>
          </section>

          <section class="term panel chart-panel">
            <div class="panel-head">
              <h2>[ PROMET UZIVO ]</h2>
              <span class="panel-hint">zadnjih ~60s</span>
            </div>
            <div class="chart-body" id="device-chart"></div>
          </section>

          <section class="term panel">
            <div class="panel-head">
              <h2>[ DNS UPITI OVOG UREDAJA ]</h2>
              <button class="log-live-btn" data-panel-key="device-page" title="Vrati na uzivo">&#8635; UZIVO</button>
            </div>
            <div class="table-wrap log-scroll" data-panel-key="device-page">
              <table>
                <thead>
                  <tr>
                    <th>Vrijeme</th>
                    <th>Domena</th>
                    <th>Status</th>
                  </tr>
                </thead>
                <tbody id="device-queries-body">
                  <tr class="empty-row"><td colspan="3">Nema jos upita...</td></tr>
                </tbody>
              </table>
            </div>
          </section>
        </main>
      </div>
    </div>

    <footer>
      DNS blokada preko WinDivert-a &middot; odspajanje = Windows Firewall &middot;
      promet/sumnjivo = heuristika, ne dokaz
    </footer>

    <div id="toast" class="term toast" role="alert" hidden></div>

    <script>
      // ---------- element refs ----------
      const devicesBody = document.getElementById("devices-body")!;
      const deviceQueriesBody = document.getElementById("device-queries-body")!;
      const clockEl = document.getElementById("clock")!;
      const liveDot = document.getElementById("live-dot")!;

      const statDevices = document.getElementById("stat-devices")!;
      const statTotal = document.getElementById("stat-total")!;
      const statBlocked = document.getElementById("stat-blocked")!;
      const statSuspicious = document.getElementById("stat-suspicious")!;
      const statCut = document.getElementById("stat-cut")!;

      const barChartEl = document.getElementById("bar-chart")!;
      const areaChartEl = document.getElementById("area-chart")!;
      const deviceChartEl = document.getElementById("device-chart")!;

      const deviceNavEl = document.getElementById("device-nav")!;
      const viewOverview = document.getElementById("view-overview")!;
      const viewDevice = document.getElementById("view-device")!;
      const breadcrumb = document.getElementById("breadcrumb")!;

      const deviceTitle = document.getElementById("device-title")!;
      const deviceMac = document.getElementById("device-mac")!;
      const deviceBadges = document.getElementById("device-badges")!;
      const deviceActionBtn = document.getElementById("device-action-btn") as HTMLButtonElement;
      const deviceStatTotal = document.getElementById("device-stat-total")!;
      const deviceStatBlocked = document.getElementById("device-stat-blocked")!;
      const deviceStatRate = document.getElementById("device-stat-rate")!;

      const domainInput = document.getElementById("domain-input") as HTMLInputElement;
      const banBtn = document.getElementById("ban-btn") as HTMLButtonElement;
      const liveChips = document.getElementById("live-chips")!;

      const toast = document.getElementById("toast")!;
      let toastTimer: ReturnType<typeof setTimeout> | undefined;

      // ---------- types ----------
      interface Device {
        ip: string;
        mac: string;
        total: number;
        blockedQueries: number;
        networkBlocked: boolean;
        traffic: number[];
        bandwidthBps: number;
        suspicious: boolean;
      }

      interface QueryEntry {
        ts: number;
        client_ip: string;
        client_mac: string;
        qname: string;
        blocked: boolean;
        reason: string;
        severity: "evasion" | "confirmed" | "suspected" | "clean";
      }

      let lastDevices: Device[] = [];
      let lastQueries: QueryEntry[] = [];
      let selectedIp: string | null = null;
      let expandedIp: string | null = null;

      // ---------- helpers ----------
      function showToast(message: string, isError: boolean) {
        toast.textContent = message;
        toast.className = isError ? "term toast toast-error show" : "term toast toast-ok show";
        toast.hidden = false;
        clearTimeout(toastTimer);
        toastTimer = setTimeout(() => {
          toast.hidden = true;
        }, 3500);
      }

      function fmtTime(ts: number): string {
        return new Date(ts * 1000).toLocaleTimeString("hr-HR", { hour12: false });
      }

      function fmtRate(bps: number): string {
        if (bps > 1024 * 1024) return (bps / 1024 / 1024).toFixed(2) + " MB/s";
        if (bps > 1024) return (bps / 1024).toFixed(1) + " KB/s";
        return Math.round(bps) + " B/s";
      }

      // ---------- scroll-preserving log panels ----------
      // Any element with [data-panel-key] holding a scrollable log table
      // keeps its scroll position across the 1.5s poll re-render instead
      // of snapping back to the top - only the "UZIVO" button forces it
      // back to the newest entries.
      const panelScroll: Record<string, number> = {};

      function captureAllPanelScroll() {
        document.querySelectorAll<HTMLElement>(".log-scroll[data-panel-key]").forEach((el) => {
          const key = el.dataset.panelKey;
          if (key) panelScroll[key] = el.scrollTop;
        });
      }

      function restoreAllPanelScroll() {
        document.querySelectorAll<HTMLElement>(".log-scroll[data-panel-key]").forEach((el) => {
          const key = el.dataset.panelKey;
          if (key && panelScroll[key] !== undefined) el.scrollTop = panelScroll[key];
        });
      }

      // ---------- charts (hand-rolled SVG, no dependency) ----------
      const CHART_WIDTH = 480;
      const CHART_HEIGHT = 110;
      const CHART_PADDING = 18;

      function makeBarChart(labels: string[], values: number[], color: string): string {
        const w = CHART_WIDTH;
        const h = CHART_HEIGHT;
        const p = CHART_PADDING;
        const max = Math.max(...values, 1);
        const gap = (w - p * 2) / Math.max(values.length, 1);
        const barWidth = Math.min(gap * 0.55, 40);

        let grid = "";
        for (let g = 0; g <= 3; g++) {
          const gy = p + ((h - p * 2) / 3) * g;
          grid += `<line x1="${p}" y1="${gy.toFixed(1)}" x2="${w - p}" y2="${gy.toFixed(1)}" class="chart-grid" />`;
        }

        let bars = "";
        values.forEach((v, i) => {
          const barHeight = (v / max) * (h - p * 2);
          const x = p + i * gap + (gap - barWidth) / 2;
          const y = h - p - barHeight;
          bars += `<rect x="${x.toFixed(1)}" y="${y.toFixed(1)}" width="${barWidth.toFixed(1)}" height="${barHeight.toFixed(1)}" fill="${color}" rx="1.5" />`;
          bars += `<text x="${(x + barWidth / 2).toFixed(1)}" y="${h - p + 13}" text-anchor="middle" class="chart-label">${labels[i]}</text>`;
          if (v > 0) {
            bars += `<text x="${(x + barWidth / 2).toFixed(1)}" y="${(y - 4).toFixed(1)}" text-anchor="middle" class="chart-value">${v}</text>`;
          }
        });

        return `<svg viewBox="0 0 ${w} ${h}" class="chart-svg" preserveAspectRatio="xMidYMid meet">${grid}${bars}</svg>`;
      }

      function makeAreaChart(values: number[], color: string, fmt: (v: number) => string): string {
        const w = CHART_WIDTH;
        const h = CHART_HEIGHT;
        const p = CHART_PADDING;
        if (values.length < 2 || values.every((v) => v === 0)) {
          return `<div class="chart-empty">Nema jos podataka</div>`;
        }
        const max = Math.max(...values, 1);
        const stepX = (w - p * 2) / (values.length - 1);
        const points = values.map((v, i) => {
          const x = p + i * stepX;
          const y = h - p - (v / max) * (h - p * 2);
          return [x, y];
        });
        const linePath = points.map((pt, i) => `${i === 0 ? "M" : "L"} ${pt[0].toFixed(1)} ${pt[1].toFixed(1)}`).join(" ");
        const last = points[points.length - 1];
        const first = points[0];
        const areaPath = `${linePath} L ${last[0].toFixed(1)} ${h - p} L ${first[0].toFixed(1)} ${h - p} Z`;

        let grid = "";
        for (let g = 0; g <= 3; g++) {
          const gy = p + ((h - p * 2) / 3) * g;
          grid += `<line x1="${p}" y1="${gy.toFixed(1)}" x2="${w - p}" y2="${gy.toFixed(1)}" class="chart-grid" />`;
        }

        return `<svg viewBox="0 0 ${w} ${h}" class="chart-svg" preserveAspectRatio="xMidYMid meet">
          ${grid}
          <path d="${areaPath}" fill="${color}" opacity="0.16" />
          <path d="${linePath}" fill="none" stroke="${color}" stroke-width="2" />
          <text x="${p}" y="13" class="chart-value">max ${fmt(max)}</text>
        </svg>`;
      }

      function renderSparkline(values: number[], suspicious: boolean): string {
        const w = 90;
        const h = 26;
        if (values.length < 2 || values.every((v) => v === 0)) {
          return `<span class="mono-dim">--</span>`;
        }
        const max = Math.max(...values, 1);
        const step = w / Math.max(values.length - 1, 1);
        const path = values
          .map((v, i) => `${i === 0 ? "M" : "L"} ${(i * step).toFixed(1)} ${(h - (v / max) * h).toFixed(1)}`)
          .join(" ");
        const stroke = suspicious ? "var(--warn)" : "var(--accent)";
        return `<svg class="spark" viewBox="0 0 ${w} ${h}" preserveAspectRatio="none">
          <path d="${path}" fill="none" stroke="${stroke}" stroke-width="1.6" stroke-linejoin="round" stroke-linecap="round" />
        </svg>`;
      }

      function aggregateTraffic(devices: Device[]): number[] {
        const maxLen = Math.max(0, ...devices.map((d) => d.traffic.length));
        const result = new Array(maxLen).fill(0);
        for (const d of devices) {
          const offset = maxLen - d.traffic.length;
          d.traffic.forEach((v, i) => {
            result[offset + i] += v;
          });
        }
        return result;
      }

      // ---------- rendering: overview ----------
      function renderBarChartPanel(devices: Device[]) {
        const sorted = [...devices].sort((a, b) => b.blockedQueries - a.blockedQueries);
        const shown = sorted.slice(0, 12);
        if (shown.length === 0 || shown.every((d) => d.blockedQueries === 0)) {
          barChartEl.innerHTML = `<div class="chart-empty">Nema blokiranih upita jos</div>`;
          return;
        }
        const labels = shown.map((d) => "." + d.ip.split(".").slice(-1)[0]);
        const values = shown.map((d) => d.blockedQueries);
        let html = makeBarChart(labels, values, "var(--danger)");
        if (sorted.length > shown.length) {
          html += `<div class="chart-note">prikazano ${shown.length} od ${sorted.length} (najvise blokiranih)</div>`;
        }
        barChartEl.innerHTML = html;
      }

      function renderAreaChartPanel(devices: Device[]) {
        areaChartEl.innerHTML = makeAreaChart(aggregateTraffic(devices), "var(--accent)", fmtRate);
      }

      function renderDeviceNav(devices: Device[]) {
        if (devices.length === 0) {
          deviceNavEl.innerHTML = `<span class="nav-empty">nema uredaja</span>`;
          return;
        }
        deviceNavEl.innerHTML = devices
          .map((d) => {
            let dot = "dot-ok";
            if (d.networkBlocked) dot = "dot-danger";
            else if (d.suspicious) dot = "dot-warn";
            const active = d.ip === selectedIp ? "active" : "";
            return `<button class="nav-item device-nav-item ${active}" data-ip="${d.ip}">
              <span class="nav-dot ${dot}"></span>${d.ip}
            </button>`;
          })
          .join("");
      }

      function severityDomainHtml(q: QueryEntry): { domainHtml: string; status: string } {
        if (q.blocked && q.severity === "evasion") {
          return {
            domainHtml: `<span class="domain-evasion">${q.qname}<span class="mark">!!</span></span>`,
            status: `<span class="badge badge-evasion">DoH ZAOBILAZI</span>`,
          };
        }
        if (q.blocked && q.severity === "confirmed") {
          return {
            domainHtml: `<span class="domain-danger">${q.qname}<span class="mark">!</span></span>`,
            status: `<span class="badge badge-danger">AI !</span>`,
          };
        }
        if (q.blocked && q.severity === "suspected") {
          return {
            domainHtml: `<span class="domain-warn">${q.qname}</span>`,
            status: `<span class="badge badge-warn">AI ?</span>`,
          };
        }
        return { domainHtml: q.qname, status: `<span class="badge badge-ok">ok</span>` };
      }

      function renderQueryRows(queries: QueryEntry[], showClient: boolean): string {
        if (queries.length === 0) {
          return `<tr class="empty-row"><td colspan="${showClient ? 4 : 3}">Nema jos upita...</td></tr>`;
        }
        return queries
          .slice(0, 30)
          .map((q) => {
            const { domainHtml, status } = severityDomainHtml(q);
            const clientCell = showClient ? `<td>${q.client_ip}</td>` : "";
            return `
              <tr>
                <td class="mono-dim">${fmtTime(q.ts)}</td>
                ${clientCell}
                <td>${domainHtml}</td>
                <td>${status}</td>
              </tr>`;
          })
          .join("");
      }

      function renderQueriesTable(tbody: HTMLElement, queries: QueryEntry[], opts: { filterIp?: string; showClient: boolean }) {
        const rows = opts.filterIp ? queries.filter((q) => q.client_ip === opts.filterIp) : queries;
        tbody.innerHTML = renderQueryRows(rows, opts.showClient);
      }

      function renderDevicesTable(devices: Device[]) {
        if (devices.length === 0) {
          devicesBody.innerHTML = `<tr class="empty-row"><td colspan="8">Nema spojenih uredaja</td></tr>`;
          return;
        }
        devicesBody.innerHTML = devices
          .map((d) => {
            const rowClass = [d.blockedQueries > 0 ? "flagged" : "", d.suspicious ? "suspicious-row" : ""]
              .join(" ")
              .trim();
            const accessLabel = d.networkBlocked
              ? `<span class="badge badge-danger">ODSPOJEN</span>`
              : `<span class="badge badge-ok">OK</span>`;
            const suspiciousLabel = d.suspicious
              ? `<span class="badge badge-warn" title="Neuobicajeno velik promet - moze biti i streaming/download, provjeri rucno">SUMNJIVO</span>`
              : "";
            const btnLabel = d.networkBlocked ? "VRATI PRISTUP" : "ODSPOJI";
            const btnClass = d.networkBlocked ? "btn btn-ghost" : "btn btn-danger";
            const isExpanded = d.ip === expandedIp;
            const expandIcon = isExpanded ? "▾" : "▸";

            let row = `
              <tr class="${rowClass}">
                <td><button class="expand-toggle" data-ip="${d.ip}" title="Prikazi/sakrij log">${expandIcon}</button></td>
                <td><button class="device-ip-link" data-ip="${d.ip}">${d.ip}</button></td>
                <td class="mono-dim">${d.mac}</td>
                <td>
                  ${renderSparkline(d.traffic, d.suspicious)}
                  <div class="rate-label">${fmtRate(d.bandwidthBps)}</div>
                </td>
                <td>${d.total}</td>
                <td>${d.blockedQueries}</td>
                <td>${accessLabel} ${suspiciousLabel}</td>
                <td>
                  <button class="${btnClass}" data-action="toggle-block" data-ip="${d.ip}" data-blocked="${d.networkBlocked}">
                    ${btnLabel}
                  </button>
                </td>
              </tr>`;

            if (isExpanded) {
              const deviceQueries = lastQueries.filter((q) => q.client_ip === d.ip);
              row += `
                <tr class="log-panel-row">
                  <td colspan="8" class="log-panel-cell">
                    <div class="log-panel">
                      <div class="panel-head">
                        <span>Upiti &middot; ${d.ip}</span>
                        <button class="log-live-btn" data-panel-key="${d.ip}" title="Vrati na uzivo">&#8635; UZIVO</button>
                      </div>
                      <div class="table-wrap log-scroll" data-panel-key="${d.ip}">
                        <table>
                          <thead>
                            <tr><th>Vrijeme</th><th>Domena</th><th>Status</th></tr>
                          </thead>
                          <tbody>${renderQueryRows(deviceQueries, false)}</tbody>
                        </table>
                      </div>
                    </div>
                  </td>
                </tr>`;
            }

            return row;
          })
          .join("");
      }

      function renderOverview() {
        captureAllPanelScroll();

        const devices = lastDevices;
        statDevices.textContent = String(devices.length);
        statTotal.textContent = String(devices.reduce((a, d) => a + d.total, 0));
        statBlocked.textContent = String(devices.reduce((a, d) => a + d.blockedQueries, 0));
        statSuspicious.textContent = String(devices.filter((d) => d.suspicious).length);
        statCut.textContent = String(devices.filter((d) => d.networkBlocked).length);

        renderBarChartPanel(devices);
        renderAreaChartPanel(devices);
        renderDevicesTable(devices);

        restoreAllPanelScroll();
      }

      // ---------- rendering: device detail ----------
      function renderDeviceView() {
        captureAllPanelScroll();

        const device = lastDevices.find((d) => d.ip === selectedIp);
        if (!device) {
          showOverview();
          return;
        }

        deviceTitle.textContent = device.ip;
        deviceMac.textContent = device.mac;

        const badges = [
          device.networkBlocked
            ? `<span class="badge badge-danger">ODSPOJEN</span>`
            : `<span class="badge badge-ok">ONLINE</span>`,
        ];
        if (device.suspicious) {
          badges.push(
            `<span class="badge badge-warn" title="Neuobicajeno velik promet - provjeri rucno">SUMNJIVO</span>`,
          );
        }
        deviceBadges.innerHTML = badges.join(" ");

        deviceStatTotal.textContent = String(device.total);
        deviceStatBlocked.textContent = String(device.blockedQueries);
        deviceStatRate.textContent = fmtRate(device.bandwidthBps);

        deviceChartEl.innerHTML = makeAreaChart(
          device.traffic,
          device.suspicious ? "var(--warn)" : "var(--accent)",
          fmtRate,
        );

        const btnLabel = device.networkBlocked ? "VRATI PRISTUP" : "ODSPOJI";
        deviceActionBtn.className = device.networkBlocked ? "btn btn-ghost" : "btn btn-danger";
        deviceActionBtn.textContent = btnLabel;
        deviceActionBtn.dataset.ip = device.ip;
        deviceActionBtn.dataset.blocked = String(device.networkBlocked);

        renderQueriesTable(deviceQueriesBody, lastQueries, { filterIp: device.ip, showClient: false });

        restoreAllPanelScroll();
      }

      function renderAll() {
        renderDeviceNav(lastDevices);
        if (selectedIp) {
          renderDeviceView();
        } else {
          renderOverview();
        }
      }

      // ---------- view switching ----------
      function showOverview() {
        selectedIp = null;
        viewOverview.hidden = false;
        viewDevice.hidden = true;
        document.querySelector('[data-view="overview"]')?.classList.add("active");
        breadcrumb.textContent = "PREGLED";
        renderAll();
      }

      function showDevice(ip: string) {
        selectedIp = ip;
        viewOverview.hidden = true;
        viewDevice.hidden = false;
        document.querySelector('[data-view="overview"]')?.classList.remove("active");
        breadcrumb.textContent = `UREDAJ / ${ip}`;
        renderAll();
      }

      // ---------- data polling ----------
      async function refresh() {
        try {
          const [devicesRes, queriesRes] = await Promise.all([fetch("/api/devices"), fetch("/api/queries")]);
          lastDevices = await devicesRes.json();
          lastQueries = await queriesRes.json();
          renderAll();
          liveDot.classList.remove("dot-error");
        } catch {
          liveDot.classList.add("dot-error");
        }
      }

      // ---------- blocklist manager ----------
      function renderChips(live: string[]) {
        if (live.length === 0) {
          liveChips.innerHTML = `<span class="chip-empty">Nema jos dodanih domena</span>`;
          return;
        }
        liveChips.innerHTML = live
          .map(
            (d) => `
              <span class="chip">
                ${d}
                <button class="chip-remove" data-domain="${d}" title="Ukloni">&times;</button>
              </span>`,
          )
          .join("");
      }

      async function refreshBlocklist() {
        try {
          const res = await fetch("/api/blocklist");
          const data = await res.json();
          renderChips(data.live || []);
        } catch {
          // silent - next poll retries
        }
      }

      async function banDomain() {
        const domain = domainInput.value.trim();
        if (!domain) return;
        banBtn.disabled = true;
        try {
          const res = await fetch("/api/blocklist", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ domain }),
          });
          const body = await res.json().catch(() => ({}));
          if (!res.ok) {
            showToast(body.error || "Neispravna domena.", true);
          } else {
            showToast(`${body.domain} - banovan`, false);
            domainInput.value = "";
          }
        } catch {
          showToast("Server ne odgovara.", true);
        } finally {
          banBtn.disabled = false;
          refreshBlocklist();
        }
      }

      banBtn.addEventListener("click", banDomain);
      domainInput.addEventListener("keydown", (event) => {
        if (event.key === "Enter") banDomain();
      });

      // ---------- delegated click handling (content is rebuilt often) ----------
      async function setBlocked(ip: string, block: boolean): Promise<boolean> {
        const endpoint = block ? "/api/block" : "/api/unblock";
        try {
          const res = await fetch(endpoint, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ ip }),
          });
          const body = await res.json().catch(() => ({}));
          if (!res.ok) {
            showToast(body.error || "Akcija nije uspjela.", true);
            return false;
          }
          showToast(block ? `${ip} - odspojen` : `${ip} - pristup vracen`, false);
          return true;
        } catch {
          showToast("Server ne odgovara.", true);
          return false;
        }
      }

      document.body.addEventListener("click", async (event) => {
        const target = event.target as HTMLElement;

        const overviewNav = target.closest('[data-view="overview"]');
        if (overviewNav) {
          showOverview();
          return;
        }

        const deviceNavBtn = target.closest(".device-nav-item[data-ip]") as HTMLButtonElement | null;
        if (deviceNavBtn) {
          showDevice(deviceNavBtn.dataset.ip!);
          return;
        }

        const ipLink = target.closest(".device-ip-link[data-ip]") as HTMLButtonElement | null;
        if (ipLink) {
          showDevice(ipLink.dataset.ip!);
          return;
        }

        const expandBtn = target.closest("button.expand-toggle[data-ip]") as HTMLButtonElement | null;
        if (expandBtn) {
          const ip = expandBtn.dataset.ip!;
          expandedIp = expandedIp === ip ? null : ip;
          renderOverview();
          return;
        }

        const liveBtn = target.closest("button.log-live-btn[data-panel-key]") as HTMLButtonElement | null;
        if (liveBtn) {
          const key = liveBtn.dataset.panelKey!;
          const el = document.querySelector<HTMLElement>(`.log-scroll[data-panel-key="${key}"]`);
          if (el) el.scrollTop = 0;
          return;
        }

        const blockBtn = target.closest("button[data-action='toggle-block']") as HTMLButtonElement | null;
        if (blockBtn) {
          const ip = blockBtn.dataset.ip!;
          const currentlyBlocked = blockBtn.dataset.blocked === "true";
          blockBtn.disabled = true;
          const ok = await setBlocked(ip, !currentlyBlocked);
          blockBtn.disabled = false;
          if (ok) refresh();
          return;
        }

        const chipRemoveBtn = target.closest("button[data-domain]") as HTMLButtonElement | null;
        if (chipRemoveBtn) {
          const domain = chipRemoveBtn.dataset.domain!;
          chipRemoveBtn.disabled = true;
          await fetch("/api/blocklist", {
            method: "DELETE",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ domain }),
          });
          showToast(`${domain} - uklonjen s live liste`, false);
          refreshBlocklist();
        }
      });

      // ---------- clock ----------
      function tickClock() {
        clockEl.textContent = new Date().toLocaleTimeString("hr-HR", { hour12: false });
      }
      tickClock();
      setInterval(tickClock, 1000);

      // ---------- boot ----------
      refresh();
      refreshBlocklist();
      setInterval(refresh, 1500);
      setInterval(refreshBlocklist, 5000);
    </script>

    <style is:global>
      /* is:global: most content on this page (tables, badges, chips,
         charts) is rendered client-side via innerHTML, so Astro's
         default scoped styles - which only tag elements present at
         build time - never match it. */
      :root {
        --bg: #fbf3d9;
        --bg-2: #f5e9c8;
        --panel-bg: rgba(255, 250, 235, 0.6);
        --panel-bg-2: rgba(244, 231, 197, 0.7);
        --shell-bg: rgba(255, 253, 242, 0.45);
        --border: rgba(122, 92, 46, 0.28);
        --text: #4a3b1f;
        --text-dim: #8a7550;
        --accent: #a8790a;
        --accent-dim: #c9a748;
        --danger: #a13a2a;
        --danger-dim: #c17a68;
        --warn: #b8752e;
        --warn-dim: #d0a06c;
        --evasion: #3b4a6b;
        --evasion-dim: #7c88a8;
        --font-mono: ui-monospace, "Cascadia Code", "Consolas", "Lucida Console",
          "SFMono-Regular", Menlo, monospace;
      }

      * {
        box-sizing: border-box;
      }

      html,
      body {
        margin: 0;
        padding: 0;
        min-height: 100%;
        background: var(--bg);
        color: var(--text);
        font-family: var(--font-mono);
      }

      body {
        padding: 14px;
      }

      .ambient-pulse {
        position: fixed;
        inset: 0;
        z-index: 0;
        pointer-events: none;
        background: radial-gradient(circle at 25% 20%, rgba(168, 121, 10, 0.12), transparent 55%),
          radial-gradient(circle at 80% 75%, rgba(161, 58, 42, 0.06), transparent 55%);
        animation: ambientPulse 9s ease-in-out infinite;
      }

      @keyframes ambientPulse {
        0%,
        100% {
          opacity: 0.55;
        }
        50% {
          opacity: 1;
        }
      }

      ::-webkit-scrollbar {
        width: 9px;
        height: 9px;
      }
      ::-webkit-scrollbar-track {
        background: transparent;
      }
      ::-webkit-scrollbar-thumb {
        background: var(--accent-dim);
        border-radius: 5px;
      }

      .app-shell {
        position: relative;
        z-index: 1;
        display: flex;
        min-height: calc(100vh - 28px);
        background: var(--shell-bg);
        backdrop-filter: blur(3px);
        -webkit-backdrop-filter: blur(3px);
        border: 1px solid var(--border);
        border-radius: 8px;
        box-shadow: 0 1px 3px rgba(74, 59, 31, 0.08);
        overflow: hidden;
      }

      .sidebar {
        width: 240px;
        flex-shrink: 0;
        display: flex;
        flex-direction: column;
        border-right: 1px solid var(--border);
        background: var(--panel-bg);
      }

      .brand {
        text-align: center;
        padding: 20px 10px;
        font-size: 19px;
        font-weight: 700;
        letter-spacing: 3px;
        color: var(--accent);
        border-bottom: 1px solid var(--border);
      }

      .nav {
        flex: 1;
        padding: 14px 10px;
        overflow-y: auto;
      }

      .nav-label {
        font-size: 10px;
        letter-spacing: 1.5px;
        color: var(--text-dim);
        margin: 16px 6px 6px;
      }

      .nav-item {
        display: block;
        width: 100%;
        text-align: left;
        background: transparent;
        border: 1px solid transparent;
        color: var(--text-dim);
        font-family: var(--font-mono);
        font-size: 12px;
        letter-spacing: 0.5px;
        padding: 8px 10px;
        margin-bottom: 4px;
        cursor: pointer;
        border-radius: 5px;
      }

      .nav-item:hover {
        color: var(--accent);
        border-color: var(--border);
      }

      .nav-item.active {
        color: var(--accent);
        border-color: var(--accent-dim);
        background: rgba(168, 121, 10, 0.08);
      }

      .device-nav-item {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 11px;
      }

      .nav-dot {
        width: 7px;
        height: 7px;
        border-radius: 50%;
        flex-shrink: 0;
      }

      .dot-ok {
        background: var(--accent);
      }

      .dot-warn {
        background: var(--warn);
      }

      .dot-danger {
        background: var(--danger);
      }

      .nav-empty {
        display: block;
        font-size: 11px;
        color: var(--text-dim);
        padding: 4px 6px;
      }

      .sidebar-status {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 12px 16px;
        border-top: 1px solid var(--border);
        font-size: 11px;
        color: var(--text-dim);
      }

      .brand-dot {
        width: 8px;
        height: 8px;
        border-radius: 50%;
        background: var(--accent);
        animation: pulse 2.2s ease-in-out infinite;
      }

      .brand-dot.dot-error {
        background: var(--danger);
      }

      @keyframes pulse {
        0%,
        100% {
          opacity: 1;
        }
        50% {
          opacity: 0.4;
        }
      }

      .content {
        flex: 1;
        min-width: 0;
        display: flex;
        flex-direction: column;
      }

      .topbar {
        padding: 14px 24px;
        border-bottom: 1px solid var(--border);
        background: var(--panel-bg);
      }

      .breadcrumb {
        font-size: 12px;
        letter-spacing: 1.5px;
        color: var(--text-dim);
      }

      main {
        padding: 20px;
      }

      .term {
        background: var(--panel-bg);
        backdrop-filter: blur(6px);
        -webkit-backdrop-filter: blur(6px);
        border: 1px solid var(--border);
        border-radius: 8px;
      }

      .stats {
        display: grid;
        grid-template-columns: repeat(5, 1fr);
        gap: 12px;
        margin-bottom: 20px;
      }

      .stats-compact {
        grid-template-columns: repeat(3, 1fr);
      }

      .card {
        padding: 14px 16px;
      }

      .card-label {
        font-size: 10px;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        color: var(--text-dim);
        margin-bottom: 8px;
      }

      .card-value {
        font-size: 22px;
        font-weight: 700;
        color: var(--accent);
      }

      .card-danger .card-value {
        color: var(--danger);
      }

      .card-warn .card-value {
        color: var(--warn);
      }

      .charts-row {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 14px;
        margin-bottom: 20px;
      }

      .chart-panel {
        overflow: hidden;
      }

      .chart-body {
        padding: 10px 18px 14px;
      }

      .chart-svg {
        width: 100%;
        height: auto;
        display: block;
      }

      .chart-grid {
        stroke: var(--border);
        stroke-width: 1;
      }

      .chart-label {
        fill: var(--text-dim);
        font-size: 9px;
        font-family: var(--font-mono);
      }

      .chart-value {
        fill: var(--text);
        font-size: 10px;
        font-family: var(--font-mono);
      }

      .chart-empty {
        padding: 24px 0;
        text-align: center;
        color: var(--text-dim);
        font-size: 12px;
      }

      .chart-note {
        text-align: center;
        font-size: 10px;
        color: var(--text-dim);
        margin-top: -4px;
      }

      .panel {
        margin-bottom: 20px;
        overflow: hidden;
      }

      .panel-head {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 12px 18px;
        border-bottom: 1px solid var(--border);
      }

      .panel-head h2 {
        margin: 0;
        font-size: 12px;
        letter-spacing: 1.5px;
        color: var(--accent);
      }

      .panel-hint {
        font-size: 10px;
        color: var(--text-dim);
      }

      .blocklist-form {
        display: flex;
        gap: 10px;
        padding: 16px 18px 4px;
      }

      .blocklist-form input {
        flex: 1;
        background: rgba(255, 255, 255, 0.45);
        border: 1px solid var(--border);
        border-radius: 5px;
        padding: 9px 12px;
        color: var(--text);
        font-family: var(--font-mono);
        font-size: 13px;
        outline: none;
      }

      .blocklist-form input:focus {
        border-color: var(--accent-dim);
      }

      .chip-row {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        padding: 14px 18px 18px;
      }

      .chip {
        display: inline-flex;
        align-items: center;
        gap: 4px;
        padding: 5px 6px 5px 12px;
        border-radius: 999px;
        background: rgba(168, 121, 10, 0.08);
        border: 1px solid var(--accent-dim);
        color: var(--accent);
        font-size: 12px;
      }

      .chip-remove {
        background: transparent;
        border: none;
        color: var(--text-dim);
        cursor: pointer;
        font-size: 15px;
        line-height: 1;
        padding: 2px 7px;
      }

      .chip-remove:hover {
        color: var(--danger);
      }

      .chip-empty {
        color: var(--text-dim);
        font-size: 12px;
      }

      .table-wrap {
        overflow-x: auto;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        font-size: 13px;
      }

      thead th {
        text-align: left;
        font-size: 10px;
        text-transform: uppercase;
        letter-spacing: 1px;
        color: var(--text-dim);
        padding: 10px 16px;
        background: var(--panel-bg-2);
        border-bottom: 1px solid var(--border);
      }

      tbody td {
        padding: 10px 16px;
        border-bottom: 1px solid var(--border);
        white-space: nowrap;
        vertical-align: middle;
      }

      tbody tr:last-child td {
        border-bottom: none;
      }

      tbody tr.flagged {
        background: rgba(161, 58, 42, 0.06);
      }

      tbody tr.suspicious-row {
        background: rgba(184, 117, 46, 0.07);
      }

      tbody tr.flagged.suspicious-row {
        background: linear-gradient(90deg, rgba(161, 58, 42, 0.08), rgba(184, 117, 46, 0.08));
      }

      .mono-dim {
        color: var(--text-dim);
      }

      .device-ip-link {
        background: transparent;
        border: none;
        color: var(--accent);
        font-family: var(--font-mono);
        font-size: 13px;
        text-decoration: underline;
        text-underline-offset: 3px;
        cursor: pointer;
        padding: 0;
      }

      .expand-toggle {
        background: transparent;
        border: 1px solid var(--border);
        border-radius: 4px;
        color: var(--text-dim);
        cursor: pointer;
        font-size: 11px;
        width: 22px;
        height: 22px;
        line-height: 1;
      }

      .expand-toggle:hover {
        color: var(--accent);
        border-color: var(--accent-dim);
      }

      .log-panel-cell {
        padding: 0;
        border-bottom: none;
        background: transparent;
      }

      .log-panel {
        margin: 8px 14px 14px;
        border: 1px solid var(--border);
        border-radius: 6px;
        background: rgba(255, 253, 240, 0.5);
        overflow: hidden;
        white-space: normal;
      }

      .log-panel .panel-head {
        padding: 8px 12px;
        font-size: 11px;
      }

      .log-live-btn {
        font-family: var(--font-mono);
        font-size: 10px;
        letter-spacing: 0.3px;
        background: transparent;
        border: 1px solid var(--accent-dim);
        color: var(--accent);
        padding: 3px 9px;
        border-radius: 4px;
        cursor: pointer;
      }

      .log-live-btn:hover {
        background: rgba(168, 121, 10, 0.08);
      }

      .log-scroll {
        max-height: 240px;
        overflow-y: auto;
      }

      .spark {
        display: block;
        width: 90px;
        height: 26px;
      }

      .rate-label {
        font-size: 10px;
        color: var(--text-dim);
        margin-top: 2px;
      }

      .domain-danger {
        color: var(--danger);
        font-weight: 600;
      }

      .domain-danger .mark {
        margin-left: 4px;
      }

      .domain-warn {
        color: var(--warn);
        font-weight: 600;
      }

      .domain-evasion {
        color: var(--evasion);
        font-weight: 700;
      }

      .domain-evasion .mark {
        margin-left: 4px;
      }

      .empty-row td {
        text-align: center;
        color: var(--text-dim);
        padding: 20px;
      }

      .badge {
        display: inline-block;
        padding: 2px 9px;
        border-radius: 999px;
        font-size: 10px;
        letter-spacing: 0.5px;
        border: 1px solid transparent;
      }

      .badge-ok {
        color: var(--accent);
        border-color: var(--accent-dim);
        background: rgba(168, 121, 10, 0.08);
      }

      .badge-danger {
        color: var(--danger);
        border-color: var(--danger-dim);
        background: rgba(161, 58, 42, 0.1);
      }

      .badge-warn {
        color: var(--warn);
        border-color: var(--warn-dim);
        background: rgba(184, 117, 46, 0.1);
      }

      .badge-evasion {
        color: var(--evasion);
        border-color: var(--evasion-dim);
        background: rgba(59, 74, 107, 0.1);
      }

      .btn {
        font-family: var(--font-mono);
        font-size: 11px;
        letter-spacing: 0.5px;
        padding: 6px 12px;
        border-radius: 5px;
        cursor: pointer;
        border: 1px solid transparent;
        transition: opacity 0.15s ease;
      }

      .btn:hover {
        opacity: 0.85;
      }

      .btn:disabled {
        opacity: 0.4;
        cursor: default;
      }

      .btn-danger {
        background: rgba(161, 58, 42, 0.12);
        color: var(--danger);
        border-color: var(--danger-dim);
      }

      .btn-ghost {
        background: transparent;
        color: var(--accent);
        border-color: var(--accent-dim);
      }

      .btn-accent {
        background: rgba(168, 121, 10, 0.14);
        color: var(--accent);
        border-color: var(--accent-dim);
        padding: 9px 18px;
      }

      .device-header {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        margin-bottom: 18px;
        flex-wrap: wrap;
        gap: 10px;
      }

      .device-header h2 {
        margin: 0 0 4px;
        color: var(--accent);
        font-size: 19px;
      }

      .device-header-right {
        display: flex;
        align-items: center;
        gap: 10px;
      }

      footer {
        position: relative;
        z-index: 1;
        text-align: center;
        padding: 14px;
        font-size: 11px;
        color: var(--text-dim);
      }

      .toast {
        position: fixed;
        left: 50%;
        bottom: 28px;
        transform: translateX(-50%) translateY(12px);
        z-index: 10;
        padding: 10px 18px;
        border-radius: 6px;
        font-size: 12px;
        letter-spacing: 0.3px;
        color: var(--text);
        opacity: 0;
        transition: opacity 0.2s ease, transform 0.2s ease;
        pointer-events: none;
      }

      .toast.show {
        opacity: 1;
        transform: translateX(-50%) translateY(0);
      }

      .toast-ok {
        color: var(--accent);
        border-color: var(--accent-dim);
      }

      .toast-error {
        color: var(--danger);
        border-color: var(--danger-dim);
      }

      @media (max-width: 900px) {
        .app-shell {
          flex-direction: column;
        }
        .sidebar {
          width: 100%;
          flex-direction: row;
          flex-wrap: wrap;
          align-items: center;
        }
        .brand {
          border-bottom: none;
          border-right: 1px solid var(--border);
          padding: 12px 18px;
        }
        .nav {
          display: flex;
          align-items: center;
          gap: 8px;
          overflow-x: auto;
          padding: 8px 10px;
          flex: 1;
        }
        .nav-item {
          width: auto;
          flex-shrink: 0;
          white-space: nowrap;
          margin-bottom: 0;
        }
        .nav-label {
          margin: 0 4px;
          white-space: nowrap;
          flex-shrink: 0;
        }
        #device-nav {
          display: flex;
          gap: 8px;
          flex-shrink: 0;
        }
        .sidebar-status {
          border-top: none;
          border-left: 1px solid var(--border);
        }
        .stats,
        .charts-row {
          grid-template-columns: repeat(2, 1fr);
        }
      }

      @media (max-width: 560px) {
        .stats,
        .stats-compact,
        .charts-row {
          grid-template-columns: 1fr;
        }
        .blocklist-form {
          flex-direction: column;
        }
      }
    </style>
  </body>
</html>
